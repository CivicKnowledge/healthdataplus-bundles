
To generate the bundle:

1) clean the bundle

2) Exec meta_copy_partitions

3) bambry schenma -s to generate the source schema. DOn't ingest first, the schema is generated from the partitions. 

4) bambry schema -D to generate the destinatino schema. 

